<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:ignore="WrongFolder">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/brand_saffron_dark"
            app:title="@string/nursing_studio"
            app:titleTextColor="@android:color/white"
            app:navigationIcon="@drawable/ic_menu_24"
            app:navigationIconTint="@android:color/white"
            app:titleCentered="false"
            app:titleTextAppearance="@style/ToolbarTitleBold"
            android:elevation="4dp"
            app:materialThemeOverlay="@style/ThemeOverlay.Material3.Dark.ActionBar" />

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_graph" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/surface_card"
            app:itemIconTint="@color/brand_blue"
            app:itemTextColor="@color/brand_blue"
            app:menu="@menu/bottom_nav_menu"
            tools:ignore="UnusedAttribute,TabletLayout" />

    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/surface_card"
        app:headerLayout="@layout/drawer_header"
        app:menu="@menu/drawer_menu"
        tools:ignore="NavigationViewLayout" />

</androidx.drawerlayout.widget.DrawerLayout>