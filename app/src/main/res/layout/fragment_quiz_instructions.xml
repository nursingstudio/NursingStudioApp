<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/app_background">

    <!-- 1. Header Title -->
    <TextView
        android:id="@+id/tvInstructionTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/test_instructions"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/brand_blue" />

    <!-- 2. Scrollable Instructions Card -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp">

        <ScrollView
            android:id="@+id/scrollViewInstructions"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            android:fillViewport="true">

            <TextView
                android:id="@+id/tvInstructionsContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="15sp"
                android:lineSpacingExtra="4dp"
                android:textColor="@color/text_primary" />
        </ScrollView>
    </com.google.android.material.card.MaterialCardView>

    <!-- 3. Agreement Checkbox (Disabled initially) -->
    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/cbAgreeTerms"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:enabled="false"
        android:text="@string/test_consent"
        android:textColor="@color/medical_teal"
        android:textSize="14sp"
        app:buttonTint="@color/medical_teal"/>

    <!-- 4. Action Button (Centered Horizontally) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnStartQuiz"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:layout_gravity="center_horizontal"
        android:paddingHorizontal="24dp"
        android:enabled="false"
        android:text="@string/i_am_ready_to_begin"
        android:textSize="16sp"
        android:textColor="@color/app_background"
        android:backgroundTint="@color/brand_saffron_dark"
        app:cornerRadius="12dp" />

</LinearLayout>